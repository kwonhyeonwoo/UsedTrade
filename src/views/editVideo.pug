extends base

block content 
    form(method="POST").upload__form
        input(name="title" type="text",placeholder="Title" value=deal.title)
        input(name="address" type="text" placeholder="Address" value=deal.address)
        input(name="price" type="text" placeholder="Price" value=deal.price)
        input(name="description" type="text" placeholder="Description" value=deal.description)
        input(type="submit",value="Edit Video")