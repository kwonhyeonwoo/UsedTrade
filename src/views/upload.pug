extends base

block content 
    if errorMessage 
        sapn=errorMessage
    form(method="POST" , enctype="multipart/form-data").upload__form
        label(for="avatar") Photo 
        input(type="file" id="avatar" name="avatar" accept="image/*") 
        input(type="text" placeholder="Title" name="title" maxlength=30)
        input(type="text" placeholder="Address" name="address" maxlength=30)
        input(type="text" placeholder="Description" name="description" maxlength=30)
        input(type="text" placeholder="Price" name="price")
        input(type="submit" value="Upload")